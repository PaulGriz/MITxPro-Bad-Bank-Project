import BootCard from "../components/IndexCard";

const Home = () => {
  return (
    <div>
      <BootCard
        title="BadBank Landing Module"
        heading="Welcome to the bank"
        text="You can navigate using the navbar."
      />
    </div>
  );
};

export default Home;
